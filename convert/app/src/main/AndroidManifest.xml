<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.convert">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Convert">
        <activity android:name=".convert_activities.Energy"></activity>
        <activity android:name=".convert_activities.Volume" />
        <activity android:name=".convert_activities.Time" />
        <activity android:name=".convert_activities.Angle" />
        <activity android:name=".convert_activities.Temperature" />
        <activity android:name=".convert_activities.Speed" />
        <activity android:name=".convert_activities.Mass" />
        <activity android:name=".convert_activities.Length" />
        <activity android:name=".convert_activities.Currency" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>